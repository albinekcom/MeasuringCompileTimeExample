os:
  - osx

osx_image:
  - xcode10.1

script:
  - xcodebuild build -project MeasuringCompileTimeExample.xcodeproj -scheme MeasuringCompileTimeExample -destination "platform=iOS Simulator,name=iPhone XS"
